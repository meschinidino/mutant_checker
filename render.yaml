services:
  - type: web
    name: fastapi-app
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    envVars:
      - key: PORT
        value: 8000  # This could be removed if Render dynamically sets the PORT
